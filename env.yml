channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - pip
  - numpy
  - scipy
  - tensorboard
  - pip:
      - tensorflow==2.18.0
      - torch==2.5.1
      - torchvision==0.20.1
      - tqdm
      - pillow
      - pyyaml
      - rich
      - requests